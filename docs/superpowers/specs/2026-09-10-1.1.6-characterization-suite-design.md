# Specification: 1.1.6 Characterization Suite and Test Infrastructure

## 1. Objective

Establish a deterministic, comprehensive characterization test suite for `django-start` version 1.1.6 and fix repository test infrastructure, adhering strictly to the principle of minimal intervention:
- `djstartlib/` remains completely immutable.
- `pyproject.toml` and `setup.cfg` packaging metadata remain untouched.
- Test infrastructure is isolated using a standalone `pytest.ini` and `requirements-test.txt`.
- `.gitignore` is fixed narrowly to allow tracking `tests/` and test configuration.
- A comprehensive Behavior Contract Matrix is documented in `docs/baseline-1.1.6.md`.

---

## 2. Infrastructure Design

### 2.1 Git Configuration (`.gitignore`)
Analysis of current `.gitignore`:
- The pattern `*test*` inadvertently ignores any file or folder containing the substring "test", including `tests/`, `pytest.ini`, `requirements-test.txt`, etc.
- The patterns `test_*` and `test/` ignore test files and directories.
- Specific test artifacts are already ignored separately: `htmlcov/`, `.tox/`, `.nox/`, `.coverage`, `.coverage.*`, `.pytest_cache/`, `cover/`.

**Action**: Remove the broad rules (`test_*`, `*test*`, `test/`) without adding unnecessary new rules, allowing test code to be tracked while maintaining ignore rules for test caches and coverage reports.

### 2.2 Test Runner Configuration (`pytest.ini`)
A standalone `pytest.ini` at repository root:
```ini
[pytest]
pythonpath = .
testpaths = tests
addopts = -v --strict-markers
markers =
    integration: tests exercising end-to-end orchestration or external boundaries
```
This avoids any modification to `setup.cfg` or `pyproject.toml`, keeping production configuration completely unpolluted.

### 2.3 Test Dependencies (`requirements-test.txt`)
Explicit test dependencies pinned to versions compatible with the runtime environment:
```text
pytest>=8.0.0
pytest-cov>=5.0.0
```

---

## 3. Characterization Test Architecture

Tests are structured under `tests/` in two distinct layers:

```text
tests/
├── conftest.py
├── unit/
│   ├── test_cli.py
│   ├── test_version.py
│   ├── test_environment.py
│   ├── test_project_manager.py
│   ├── test_app_manager.py
│   └── test_helper.py
└── integration/
    └── test_scaffold_orchestration.py
```

### 3.1 Test Principles
1. **Black-box over white-box**: Observe CLI outputs, exit codes, generated files, and file modifications rather than asserting private call order or implementation details.
2. **Deterministic & Isolated**:
   - Every file-modifying test operates inside a pytest `tmp_path` fixture with a controlled working directory.
   - External network boundaries (`urllib.request.urlopen`) and subprocess calls (`subprocess.call`) are mocked in unit tests to eliminate external dependencies.
3. **Reproduce Before Classifying**:
   - Suspected bugs (e.g. whitespace dependence in `settings.py`/`urls.py`, shell command injection risks, arithmetic version check) are characterized with exact reproducing tests.
   - Discovered quirks are classified into:
     - `intended`: Documented/expected behavior of 1.1.6.
     - `legacy-reliance`: Observable behavior that users or existing workflows may rely on.
     - `known-defect`: Demonstrably broken behavior under specific inputs.

---

## 4. Behavior Contract Matrix

The following contract matrix will be codified in `docs/baseline-1.1.6.md` and backed by tests:

| ID | Surface | Input / Precondition | Observed Behavior | Classification | Test Target |
|---|---|---|---|---|---|
| `BC-CLI-01` | CLI (`main`) | `django-start myproject myapp` | Initiates project and app creation in workdir | intended | `tests/unit/test_cli.py` |
| `BC-CLI-02` | CLI (`main`) | `django-start myproject myapp --name custom_env` | Passes custom env path converted to absolute Path | intended | `tests/unit/test_cli.py` |
| `BC-CLI-03` | CLI (`main`) | `django-start myproject myapp -v` | Prints deprecation notice for `--virtualenv` flag | legacy-reliance | `tests/unit/test_cli.py` |
| `BC-CLI-04` | CLI (`main`) | `django-start myproject myapp -u custom/path/` | Forwards custom URL prefix to app URL configuration | intended | `tests/unit/test_cli.py` |
| `BC-CLI-05` | CLI (`main`) | No arguments or missing arguments | Click exits with code 2 and usage instructions | intended | `tests/unit/test_cli.py` |
| `BC-VER-01` | CLI (`version`) | `django-version` (no options) | Prints `1.1.6 (beta)` | intended | `tests/unit/test_version.py` |
| `BC-VER-02` | CLI (`version`) | `django-version --check-update` with newer version | Sums version segments, prints `New Update Available <tag>` | legacy-reliance / known-defect | `tests/unit/test_version.py` |
| `BC-VER-03` | CLI (`version`) | `django-version --check-update` with same or older version | Prints `You have the latest version` | intended | `tests/unit/test_version.py` |
| `BC-VER-04` | CLI (`version`) | `django-version --check-update` on URLError | Exits process with code 1 | intended | `tests/unit/test_version.py` |
| `BC-VER-05` | CLI (`version`) | `django-version --update` | Invokes `pip install --upgrade django-start-automate` with `shell=True` | legacy-reliance / known-defect | `tests/unit/test_version.py` |
| `BC-ENV-01` | `Environment` | Base class initialization | Tracks app, project, workdir, `PYTHONEXEC`, `DJANGOADMIN` | intended | `tests/unit/test_environment.py` |
| `BC-ENV-02` | `Environment` | `read_file`, `insert_line`, `replace_line`, `write` | Basic line-based text manipulation | intended | `tests/unit/test_environment.py` |
| `BC-PRJ-01` | `ProjectManager` | `create_project()` when dir not present | Executes `django-admin startproject <name> .` | intended | `tests/unit/test_project_manager.py` |
| `BC-PRJ-02` | `ProjectManager` | `create_project()` when dir exists | Warns `<name> Already exist!` without command execution | intended | `tests/unit/test_project_manager.py` |
| `BC-PRJ-03` | `ProjectManager` | `update_settings()` with standard `settings.py` | Inserts app name before `]\n` | legacy-reliance | `tests/unit/test_project_manager.py` |
| `BC-PRJ-04` | `ProjectManager` | `update_settings()` when app already in settings | Warns `<app> is installed!` and does not duplicate | intended | `tests/unit/test_project_manager.py` |
| `BC-PRJ-05` | `ProjectManager` | `update_import_statment()` in `urls.py` | Inserts `include` statement above `path` statement | legacy-reliance | `tests/unit/test_project_manager.py` |
| `BC-PRJ-06` | `ProjectManager` | `update_urls()` with standard `urls.py` | Inserts app path routing before `]\n` | legacy-reliance | `tests/unit/test_project_manager.py` |
| `BC-PRJ-07` | `ProjectManager` | `update_urls()` when route already present | Warns `Urls Already Updated` | intended | `tests/unit/test_project_manager.py` |
| `BC-APP-01` | `AppManager` | `create_app()` when dir not present | Executes `python manage.py startapp <app>` | intended | `tests/unit/test_app_manager.py` |
| `BC-APP-02` | `AppManager` | `create_app()` when dir exists | Warns `<app> already exist!` without command execution | intended | `tests/unit/test_app_manager.py` |
| `BC-APP-03` | `AppManager` | `update_view()` with `# Create your views here.\n` | Replaces comment line with `home` view function | legacy-reliance | `tests/unit/test_app_manager.py` |
| `BC-APP-04` | `AppManager` | `update_view()` when comment missing | Warns `"home" View is Exists!` without modification | legacy-reliance / known-defect | `tests/unit/test_app_manager.py` |
| `BC-APP-05` | `AppManager` | `create_urls()` | Writes app `urls.py` with `urlpatterns = [path('', views.home)]` | intended | `tests/unit/test_app_manager.py` |
| `BC-APP-06` | `AppManager` | `create_templates()` | Generates `templates/<app>/index.html` | intended | `tests/unit/test_app_manager.py` |
| `BC-APP-07` | `AppManager` | `create_templates()` when template already exists | Warns `Index.html is Already exists.` | intended | `tests/unit/test_app_manager.py` |
| `BC-HLP-01` | `helper` | `build_view_func()`, `build_views_urls()`, `generate_html()` | Produces expected code and HTML string templates | intended | `tests/unit/test_helper.py` |
| `BC-HLP-02` | `helper` | `create_env()`, `upgrade_pip()`, `install_dep()`, `requirements_extract()` | Executes expected shell/subprocess commands and sets env vars | legacy-reliance | `tests/unit/test_helper.py` |
| `BC-ORCH-01` | `DjangoStart` | Full pipeline execution (`setup_project`, `setup_app`) | Coordinates project creation, app creation, and file wiring in order | intended | `tests/integration/test_scaffold_orchestration.py` |

---

## 5. Documentation Deliverable (`docs/baseline-1.1.6.md`)

The baseline document will include:
1. Environment and historical platform assumptions of 1.1.6 (Python version target, packaging setup).
2. The complete Behavior Contract Matrix.
3. Observed defects and quirks reproduced by the test suite (e.g. `sum(var_int)` version comparison flaw, `line_list.index("]\n")` fragility if formatting differs, unescaped shell commands).
4. Safety net guarantees for upcoming 2.0 modernization.
